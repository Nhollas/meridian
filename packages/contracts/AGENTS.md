# @meridian/contracts

Shared Zod schemas and TypeScript types used across API, chat, and CLI packages.

## Commands

```bash
pnpm test       # unit tests
pnpm typecheck  # type checking
```

## Structure

- Runtime event contracts in `src/runtime-events/` — defines the event protocol between API and consumers
